import { useForm } from '@inertiajs/react'
export default function Login({ error }: { error?: string }) { const { data, setData, post, processing } = useForm({ username: '', password: '' }); return <main className="grid min-h-screen place-items-center bg-slate-950 p-5"><form onSubmit={e => {e.preventDefault(); post('/connexion/')}} className="w-full max-w-md rounded-2xl bg-white p-8 shadow-xl"><h1 className="text-2xl font-black">ETIGE<span className="text-amber-500">.Manager</span></h1><p className="mb-7 mt-2 text-sm text-slate-500">Connectez-vous à votre espace de pilotage.</p>{error && <p className="mb-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<label className="text-sm font-medium">Identifiant<input className="input" value={data.username} onChange={e => setData('username', e.target.value)} required /></label><label className="mt-4 block text-sm font-medium">Mot de passe<input type="password" className="input" value={data.password} onChange={e => setData('password', e.target.value)} required /></label><button disabled={processing} className="btn-primary mt-7 w-full">Se connecter</button></form></main> }

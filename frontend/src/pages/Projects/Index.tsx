import AppLayout from '../../layouts/AppLayout'
import { Link } from '@inertiajs/react'
import Status from '../../components/Status'
export default function Projects({ projects }: any) { return <AppLayout><div className="mb-7 flex items-center justify-between"><div><h2 className="text-3xl font-bold">Projets</h2><p className="text-slate-500">Suivez chaque projet de l’étude à la clôture.</p></div><Link href="/projets/nouveau/" className="btn-primary">+ Nouveau projet</Link></div><div className="grid gap-4 md:grid-cols-2">{projects.map((p: any) => <Link key={p.id} href={`/projets/${p.id}/`} className="card transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex justify-between gap-3"><div><p className="text-xs font-bold text-amber-700">{p.reference}</p><h3 className="mt-1 text-lg font-bold">{p.name}</h3><p className="mt-1 text-sm text-slate-500">{p.client__company_name}</p></div><Status value={p.status} /></div><p className="mt-5 text-sm text-slate-500">Budget : <span className="font-semibold text-slate-700">{Number(p.budget).toLocaleString('fr-FR')} FCFA</span></p></Link>)}{!projects.length && <div className="card text-slate-500">Créez votre premier projet pour démarrer le workflow.</div>}</div></AppLayout> }
